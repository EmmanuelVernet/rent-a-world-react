export default function InertiaHome({ worlds }) {

  return(
    // <div>
    //   {worlds.map((world) => (
    //     <div key={world.id}>{world.name}</div>
    //   ))}
    // </div>
    <div>
      Hello world
    </div>
  )
}